# projectcontext.monomind.one
#
# Everything here is public documentation for two open-source products, and
# there is nothing on this site that should not be indexed, quoted, or used to
# answer a question. So the policy is simply: crawl it.
#
# Answer engines and model crawlers are covered by the same blanket allow
# rather than a per-agent list, because a list is a maintenance burden that
# silently excludes whichever crawler was invented after it was written.
#
# llms.txt is a Markdown index of the site written for a model reading rather
# than rendering it. Sitemap and llms.txt are both generated by
# scripts/build_site.py from the same page metadata as the pages themselves.

User-agent: *
Allow: /

Sitemap: https://projectcontext.monomind.one/sitemap.xml

# https://projectcontext.monomind.one/llms.txt
