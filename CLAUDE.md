# Project Context — host pointer

The contract for this repository is `AGENTS.md` at the root. Read it before you
do anything else here, every session.

This file is a pointer, not a copy. Nothing substantive belongs in it. If it
ever disagrees with `AGENTS.md`, `AGENTS.md` is right.

## Host notes

- Checks: `python -m unittest discover -s tests -v`, then
  `python scripts/validate_repository.py`. CI runs both.
- This repository is not a Project Context install. There is no
  `project-context/` folder here, so there is no managed block in this file and
  no records to update at the end of a session.
