# Incidents

Use this directory for evidence-backed failures whose root cause, remediation,
or prevention will help future work. Agents create records with
`project-context record --kind incident`; every record carries the six required
frontmatter keys shown in `TEMPLATE.md`. The lifecycle is `open` → `resolved`
→ `superseded`.
