# Designs

Use this directory for proposed or accepted technical and product designs.
Designs explain options and mechanics; accepted direction belongs in the
decision registry. Agents create records with `project-context record --kind
design`; every record carries the six required frontmatter keys shown in
`TEMPLATE.md`. The lifecycle is `proposed` → `accepted` → `superseded` |
`rejected`.
