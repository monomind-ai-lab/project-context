interface:
  display_name: "Initialize Project Context"
  short_description: "Add portable project memory safely"
  default_prompt: "Use $project-context-init to review this repository, suggest safe consolidations, and initialize the right project-context profile."
