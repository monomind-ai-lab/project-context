interface:
  display_name: "Update Project Context"
  short_description: "Pull a newer scaffold release without losing local changes"
  default_prompt: "Use $project-context-update to check whether a newer Project Context release exists and show me exactly what it would change before anything is written."
policy:
  allow_implicit_invocation: true
