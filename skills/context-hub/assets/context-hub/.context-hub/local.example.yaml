schema: context-hub/local@1

# context-hub:bindings:start
managed_bindings:
  - {"binding_id": "product-main", "local_path": "/absolute/path/to/local/working-copy", "project_id": "project-example"}
# context-hub:bindings:end

# This is the format maintained by `project-context hub bind-project`. Copying
# the example is optional: the command creates local.yaml safely when needed.
# Keep local.yaml untracked and never add credentials here.
