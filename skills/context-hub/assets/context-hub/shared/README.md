# Shared context

Use `shared/entities/`, `shared/relationships/`, and `shared/insights/` only for
reviewed records that intentionally apply across projects in this trust domain.
Keep project-specific candidates under their project. Every shared record uses
hub scope, lists its supporting project IDs where relevant, and links evidence.
